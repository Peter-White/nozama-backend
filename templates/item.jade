extend layout
block main
  .container.maincontent
    .row
      .col-lg-12
        .row
          .items
            each item in items
                .col-md-6
                  img(class=['img-responsive', 'img-thumbnail'] src='#{item.photoURL}' width='400px')
                .col-md-6
                  h3 #{item.itemName}
                  p #{item.description}
                  h4 Price: $ #{item.price}
                  .btn-group
                    button.btn.btn-default.buttons.add-to-cart Add To &nbsp;
                      span(class='glyphicon glyphicon-shopping-cart')
                    a.btn.btn-default.buttons(href='/items/') Keep Shopping

